## Defaults
Host *
	ControlMaster auto
	ControlPath ${XDG_RUNTIME_DIR}/ssh/sockets/%C
	ControlPersist 30
	ServerAliveInterval 60

# Settings for remote Hosts (Host example.net\n\tUser root)
Include hosts.d/*
